import { Log } from '../loggerSystem';
import { SizeFunction, SizeRange } from '../sizeSystem';
export default function useChangedChildSizes(callback: (ranges: SizeRange[]) => void, itemSize: SizeFunction, enabled: boolean, log: Log): (_el: import("./useSize").CallbackRefParam) => void;
